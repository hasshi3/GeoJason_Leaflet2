Since the operation check was completed with GeoJason_Leaflet, it was created as the first step for actual operation.
With Chat-GPT4o's suggestion, the CSS file is no longer needed. This led to a simplification of the creation process.
The list of stores that was obtained by scraping is stored in a Google spreadsheet. Longitude and latitude were added and converted to GeoJSON by Genspark.
The index.html was created by referencing that GeoJSON file.
